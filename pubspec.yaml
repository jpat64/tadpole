name: moonbase
description: "A Flutter project for tracking the lunar cycles."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.9
  go_router: ^14.0.2
  intl: ^0.19.0
  flutter_typeahead: ^5.2.0
  flutter_launcher_icons: ^0.13.1
  flutter_email_sender: ^6.0.3
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  fonts:
    - family: Freeman
      fonts:
        - asset: assets/fonts/Freeman-Regular.ttf
  assets:
    - assets/images/app icon/
    - assets/images/ui display/

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/app icon/app icon large square.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
